# Direct dependencies only — pip resolves the rest.
# Lean production set: no dev-convenience bundle (fastapi[standard]).
fastapi==0.138.0
uvicorn[standard]==0.49.0
psycopg[binary]==3.3.4
pandas==3.0.3
python-dotenv==1.2.2
